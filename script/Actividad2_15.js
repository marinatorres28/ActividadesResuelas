/* desarrolla un programa que pida un numero y calcule su factorial */
let numeroUsuario = prompt("Indica un número");
numeroL=numeroUsuario.length;

for (let i = 0; i > numeroUsuario; i++) {
    
    numeroUsuario += Number(numeroL[i])
    console.log(numeroL[i]);
}
